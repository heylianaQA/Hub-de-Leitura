{
  "dependencies": {
    "@faker-js/faker": "^9.9.0",
    "cypress": "^15.10.0",
    "cypress-plugin-api": "^2.11.2",
    "newman": "^6.2.2"
  },
  "devDependencies": {
    "wait-on": "^9.0.4"
  },
  "scripts": {
    "test:api": "newman run tests-api/collection.json --env-var baseUrl=http://localhost:3000/api/",
    "cy:run": "npx cypress run"
  }
}
